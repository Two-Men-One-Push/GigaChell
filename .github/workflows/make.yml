---
name: Make check

on:
  push:

jobs:
  norminette-check:
    runs-on: ubuntu-latest
    name: "Make Check"

    steps:
      - name: Checkout user code
        uses: actions/checkout@v3

      - name: Install Clang
        run: |
            clang --version
            sudo apt update
            sudo apt install clang=1:14.0-55

      - name: Execute Make
        run: |
            if ! make ; then
                echo "::error::Make encounter an error"
                exit 1
            fi